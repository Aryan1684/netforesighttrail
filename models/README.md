# Trained artifacts

Run setup.cmd from the repository root to download the five trained artifacts from the source model repository.

Required:
- feature_scaler.pkl
- label_encoder.pkl
- feature_names.pkl
- xgboost_model.pkl
- transformer_forecaster.pt

Binary artifacts are intentionally downloaded during setup rather than duplicated in source control.
